apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itau-service-provider.fullname" . }}-test-connection"
  labels:
    {{- include "itau-service-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itau-service-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
