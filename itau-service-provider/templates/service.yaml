apiVersion: v1
kind: Service
metadata:
  name: {{ include "itau-service-provider.fullname" . }}
  labels:
    {{- include "itau-service-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "itau-service-provider.selectorLabels" . | nindent 4 }}
