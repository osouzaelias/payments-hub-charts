apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-connector.fullname" . }}-test-connection"
  labels:
    {{- include "http-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "http-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
